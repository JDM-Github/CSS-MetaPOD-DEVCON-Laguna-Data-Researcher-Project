import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import WAISMainApplication from './WAISMainApplication.tsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <WAISMainApplication />
  </React.StrictMode>
);
